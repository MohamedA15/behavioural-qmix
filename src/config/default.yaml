# --- Defaults ---

# --- pymarl options ---
runner: "episode" # Runs 1 env for an episode
mac: "basic_mac" # Basic controller
env: "sc2" # Environment name
common_reward: True  # Run experiment with common reward setup
reward_scalarisation: "sum"  # How to aggregate rewards to single common reward (only used if common_reward is True)
env_args: {} # Arguments for the environment
batch_size_run: 1 # Number of environments to run in parallel
test_nepisode: 20 # Number of episodes to test for
test_interval: 2000 # Test after {} timesteps have passed
test_greedy: True # Use greedy evaluation (if False, will set epsilon floor to 0)
log_interval: 2000 # Log summary of stats after every {} timesteps
runner_log_interval: 2000 # Log runner stats (not test stats) every {} timesteps
learner_log_interval: 2000 # Log training stats every {} timesteps
t_max: 10000 # Stop running after this many timesteps
use_cuda: True # Use gpu by default unless it isn't available
buffer_cpu_only: True # If true we won't keep all of the replay buffer in VRAM

# --- Logging options ---
use_tensorboard: False # Log results to TensorBoard
use_wandb: False # Log results to W&B
wandb_team: null
wandb_project: null
wandb_mode: "offline"
wandb_save_model: False
save_model: False
save_model_interval: 50000
checkpoint_path: ""
evaluate: False
render: False
load_step: 0
save_replay: False
local_results_path: "results"

# --- RL hyperparameters ---
gamma: 0.99
batch_size: 32 # Number of episodes to train on
buffer_size: 32 # Size of the replay buffer
lr: 0.0005 # Learning rate for agents
optim_alpha: 0.99 # RMSProp alpha
optim_eps: 0.00001 # RMSProp epsilon
grad_norm_clip: 10 # Reduce magnitude of gradients above this L2 norm
add_value_last_step: True

# --- Constraint weights ---
lambda_part: 0.1          # Constraint A 
lambda_interference: 0.1  # Constraint B 
# lambda_commit: 0.1          # Constraint C
lambda_target: 0.1           # Constraint D

# --- Environment ---
grid_size: 6 # Grid size for the foraging environment

# --- Agent parameters ---
agent: "rnn" # Default RNN agent
hidden_dim: 64 # Size of hidden state
obs_agent_id: True # Include the agent's one-hot ID in the observation
obs_last_action: True # Include the agent's last action in the observation

# --- Experiment running params ---
repeat_id: 1
label: "default_label"
experiment_suffix: ""
hypergroup: null